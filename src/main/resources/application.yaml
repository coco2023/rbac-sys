
server:
  port: 9333
  servlet:
    context-path: /api/

spring:
  datasource:
    driverClassName: org.h2.Driver
    username: root
    password: 12345
#    platform: h2
    url: jdbc:h2:mem:jwt-demo
    # http://localhost:9333/api/h2-console
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    show-sql: true
  cache:
    # ???Spring Cache?????spring.cache.type???????????????????Cache???????????Redis??????JCache -> EhCache -> Redis -> Guava?
    type: REDIS
  redis:
    host: 127.0.0.1
    port: 6379
    # password:  ??????????????????
    # ???????ms?
    timeout: 10000
    # Redis??????16???????????????????0
    database: 0
    jedis:
      pool:
        # ???????????????????? ?? 8
        max-active: 10
        # ??????????????????????? ?? -1
        max-wait: -1
        # ??????????? ?? 8
        max-idle: 8
        # ??????????? ?? 0
        min-idle: 0
